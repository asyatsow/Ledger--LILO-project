{"off_by_one":[{"id":"oob_1","prompt":"You have a list of 10 items and want to loop through every index. What should the range/condition be, and why is range(len(list) + 1) wrong?","expected_understanding":"Valid indices go from 0 to len-1; len(list)+1 attempts one past the last valid index and can cause IndexError."},{"id":"oob_2","prompt":"A while loop meant to process a queue keeps running one extra time and crashes on the last item. What's the likely bug in the loop condition, and how would you fix it?","expected_understanding":"The boundary condition is off by one, such as <= instead of <, and should stop before the invalid index/state."}],"null_handling":[{"id":"null_1","prompt":"A function get_user(id) sometimes returns None if the user isn't found. What should you check before accessing .name on the result, and why?","expected_understanding":"Check that the result is not None before accessing attributes, avoiding AttributeError."},{"id":"null_2","prompt":"Why is calling user.profile.email unsafe if profile can be None, and what is a robust way to handle it?","expected_understanding":"An intermediate value can be None; guard it explicitly or use safe optional handling before dereferencing."}],"scope_error":[{"id":"scope_1","prompt":"A variable created inside a for loop is unexpectedly unavailable inside another function. What concept explains this and how would you fix it?","expected_understanding":"The variable is outside the relevant scope; pass it as an argument, return it, or define it in an accessible scope."},{"id":"scope_2","prompt":"A function raises NameError because it tries to use a local variable that was only assigned inside an if branch. What is happening?","expected_understanding":"The variable may not be initialized in the current scope/control path; initialize it before the branch or ensure every path assigns it."}],"type_mismatch":[{"id":"type_1","prompt":"Why does 5 + '5' fail in Python, and what are two sensible ways to fix it depending on the intended data?","expected_understanding":"The operands have incompatible types; convert the string to int for arithmetic or convert the number to string for concatenation."},{"id":"type_2","prompt":"A function expects a list but receives a dictionary and later indexes it like a list. What should you check first?","expected_understanding":"Verify the runtime type and the function's contract; convert or change the operation to match the intended structure."}],"logic_error":[{"id":"logic_1","prompt":"A function returns True when two values are different, but its name says it checks equality. What kind of bug is this and how would you reason about fixing it?","expected_understanding":"The program runs but implements the wrong condition; inspect the intended invariant/requirements and correct the boolean logic."},{"id":"logic_2","prompt":"A search function skips the correct answer because it updates the wrong pointer after a comparison. Why might this be a logic error rather than a syntax error?","expected_understanding":"The code is valid but follows the wrong algorithmic decision path; fix the condition/update rule based on the intended invariant."}]}
