'use client';
import Link from 'next/link'; import type {Debt,Concept} from '@/lib/types'; import {CONCEPT_LABELS} from '@/lib/types'; import {conceptOrder} from '@/lib/demo'; import {stats} from '@/lib/store';
export function ConceptTable({debts}:{debts:Debt[]}){return <div className="card overflow-hidden"><div className="grid grid-cols-[1.6fr_.7fr_1fr_auto] gap-4 px-5 py-3 border-b border-[var(--line)] text-[10px] tracking-[.14em] text-[#85827a]"><div>CONCEPT</div><div>DEPENDENCY</div><div>STATUS</div><div></div></div>{conceptOrder.map((c:Concept)=>{const s=stats(debts,c);const status=s.dependency<30?'READY':s.dependency<=60?'AI-DEPENDENT':'HIGH RISK';return <div key={c} className="grid grid-cols-[1.6fr_.7fr_1fr_auto] gap-4 items-center px-5 py-4 border-b last:border-0 border-[var(--line)] text-sm"><div className="font-semibold">{CONCEPT_LABELS[c]}</div><div className="font-black">{s.dependency}%</div><div><span className={`inline-block px-2 py-1 text-[10px] font-bold ${status==='READY'?'bg-[var(--accent)]':status==='AI-DEPENDENT'?'bg-[var(--warning)]':'bg-[var(--danger)]'}`}>{status}</span></div><div>{s.open>0&&<Link href={`/test/${c}`} className="text-xs underline">TEST →</Link>}</div></div>})}</div>}
